name: Type Checking 🔰

on:
  push:
    branches:
      - main
    paths:
      - 'black_knight/**/*.py'

jobs:
  mypy:
    name: run MyPy 🐍
    runs-on: ubuntu-latest
    steps:
      - run: echo 'test!'
